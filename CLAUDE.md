# Claude Code adapter

@AGENTS.md

Shared skills live in `.agents/skills/`; `.claude/skills/` contains relative directory links for Claude Code discovery. Edit the shared source, not a second copy. Use `/management`, `/telegram`, `/booking`, `/web`, `/squash-task`, or `/squash-review` when relevant. Project workflow and safety rules are in the imported guide; model selection remains a user choice.
